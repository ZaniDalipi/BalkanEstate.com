db.getCollectionNames().forEach(function(coll) { db[coll].getIndexes().forEach(function(idx) { if (idx.name !== '_id_') { var k = JSON.stringify(idx.key); var o = {}; if (idx.unique) o.unique = true; if (idx.sparse) o.sparse = true; if (idx.expireAfterSeconds !== undefined) o.expireAfterSeconds = idx.expireAfterSeconds; if (idx.partialFilterExpression) o.partialFilterExpression = idx.partialFilterExpression; var s = Object.keys(o).length ? ", " + JSON.stringify(o) : ""; print("db." + coll + ".createIndex(" + k + s + ")"); } }); });
