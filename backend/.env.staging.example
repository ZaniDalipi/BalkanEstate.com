# =============================================================================
# Backend Staging Environment Variables
# =============================================================================
# Copy this file to .env.staging and fill in the values
# Loaded when NODE_ENV=staging

NODE_ENV=staging
PORT=5001

# =============================================================================
# Database
# =============================================================================
# Use a separate staging database to avoid affecting production data
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/balkanestate-staging?retryWrites=true&w=majority

# =============================================================================
# Authentication
# =============================================================================
JWT_SECRET=<staging-jwt-secret>
JWT_REFRESH_SECRET=<staging-jwt-refresh-secret>

# =============================================================================
# Security Keys (generate unique values for staging)
# =============================================================================
ENCRYPTION_KEY=<staging-encryption-key-32-chars>
FIELD_ENCRYPTION_KEY=<staging-field-encryption-key-32-chars>
PASSWORD_PEPPER=<staging-password-pepper>
FINGERPRINT_SECRET=<staging-fingerprint-secret>

# =============================================================================
# URLs
# =============================================================================
FRONTEND_URL=https://staging.balkanestateai.com
BACKEND_URL=https://staging-api.balkanestateai.com
ALLOWED_ORIGINS=https://staging.balkanestateai.com,https://preview.balkanestateai.pages.dev

# =============================================================================
# Cloudinary (can share with production or use separate account)
# =============================================================================
CLOUDINARY_CLOUD_NAME=<cloud-name>
CLOUDINARY_API_KEY=<api-key>
CLOUDINARY_API_SECRET=<api-secret>

# =============================================================================
# Email (Resend)
# =============================================================================
RESEND_API_KEY=<staging-resend-api-key>
EMAIL_FROM=noreply@staging.balkanestateai.com

# =============================================================================
# AI Services
# =============================================================================
GEMINI_API_KEY=<gemini-api-key>

# =============================================================================
# OAuth (Google) - use staging redirect URIs
# =============================================================================
GOOGLE_CLIENT_ID=<google-client-id>
GOOGLE_CLIENT_SECRET=<google-client-secret>

# =============================================================================
# Payments (Stripe) - use TEST mode keys
# =============================================================================
STRIPE_SECRET_KEY=sk_test_<your-test-key>
STRIPE_WEBHOOK_SECRET=whsec_<your-test-webhook-secret>
STRIPE_PUBLISHABLE_KEY=pk_test_<your-test-key>

# =============================================================================
# Monitoring (optional in staging)
# =============================================================================
# SENTRY_DSN=
