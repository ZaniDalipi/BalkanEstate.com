// PostCSS config for Tailwind v4 with @tailwindcss/vite
// Note: @tailwindcss/vite handles CSS processing, so we don't need
// postcss-import or tailwindcss plugins here - they would conflict.
export default {
  plugins: {}
}
