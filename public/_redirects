# Cloudflare Pages SPA redirect
# Note: Cloudflare Pages automatically serves index.html for SPA routes
# The catch-all rule /* /index.html 200 causes an infinite loop in
# Cloudflare Pages and must NOT be used here.
# Custom redirects can be added below if needed in the future.
