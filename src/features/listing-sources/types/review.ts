/** Whether the full-size review shows the listing or the editor beside it. */
export type ReviewMode = 'preview' | 'edit';

/** What the owner decides about a draft. */
export type ReviewDecision = 'accept' | 'reject' | 'restore';
